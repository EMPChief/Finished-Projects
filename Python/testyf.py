# The code `print (range(0,11, 2))` is creating a range object that starts at 0, ends at 11
# (exclusive), and increments by 2. The `print` function is then used to display the range object.
print (range(0,11, 2))